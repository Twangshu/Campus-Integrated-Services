
<view class='tab'>
         <van-tabs z-index='0' active="{{ active }}" bind:change="onChange">
  <van-tab title="杂谈">
<swiper class="swiper" indicator-dots="{{indicatorDots}}"
  autoplay="{{autoplay}}" interval="{{interval}}" duration="{{duration}}">
  <block wx:for="{{test}}"wx:for-index="idx">
    <swiper-item bindtap="onReplyClick">
      <view class="bg">
  </view>
      <text space="ensp" class="textArea">    {{item}}</text>
      <view class="line"></view>
       <text space="ensp" class="reply">                                         阅读：{{read1[idx]}}   回复Ta</text>

    
    </swiper-item>
  </block>
     
</swiper>
 <button class="btn">我也要说</button>

  </van-tab>
  <van-tab title="求组队">
<swiper class="swiper" indicator-dots="{{indicatorDots}}"
  autoplay="{{autoplay}}" interval="{{interval}}" duration="{{duration}}">
  <block wx:for="{{test2}}"wx:for-index="idx">
    <swiper-item bindtap="onReplyClick">
      <view class="bg">
  </view>
      <text space="ensp" class="textArea">    {{item}}</text>
      <view class="line"></view>
       <text space="ensp" class="reply">                                         阅读：{{read2[idx]}}   回复Ta</text>

    
    </swiper-item>
  </block>
     
</swiper>
 <button class="btn">我也要说</button>



  </van-tab>
    <van-tab title="求交易">
  <swiper class="swiper" indicator-dots="{{indicatorDots}}"
  autoplay="{{autoplay}}" interval="{{interval}}" duration="{{duration}}">
  <block wx:for="{{test3}}"wx:for-index="idx">
    <swiper-item bindtap="onReplyClick">
      <view class="bg">
  </view>
      <text space="ensp" class="textArea">    {{item}}</text>
      <view class="line"></view>
       <text space="ensp" class="reply">                                         阅读：{{read3[idx]}}   回复Ta</text>

    
    </swiper-item>
  </block>
     
</swiper>
 <button class="btn">我也要说</button>

  </van-tab>
    <van-tab title="求脱单">
  <swiper class="swiper" indicator-dots="{{indicatorDots}}"
  autoplay="{{autoplay}}" interval="{{interval}}" duration="{{duration}}">
  <block wx:for="{{test4}}"wx:for-index="idx">
    <swiper-item bindtap="onReplyClick">
      <view class="bg">
  </view>
      <text space="ensp" class="textArea">    {{item}}</text>
      <view class="line"></view>
       <text space="ensp" class="reply">                                         阅读：{{read4[idx]}}   回复Ta</text>

    
    </swiper-item>
  </block>
     
</swiper>
 <button class="btn">我也要说</button>

  </van-tab>
</van-tabs>
</view>